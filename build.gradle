// File: build.gradle (Project level)
buildscript {
    ext {
        compose_version = '1.4.3' // Sostituisci con l'ultima versione disponibile
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2' // Sostituisci con l'ultima versione disponibile
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.10' // Sostituisci con l'ultima versione disponibile
    }
}
